generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model User {
  id       String   @id @default(auto()) @map("_id") @db.ObjectId
  name     String
  email    String   @unique
  password String
  role     String
  job      String[]
}

model Sejarah {
  id      String  @id @default(auto()) @map("_id") @db.ObjectId
  title   String
  excerpt String?
  image   String
  content String?
  userId  String
  view    Int?
}

model VisiMisi {
  id      String  @id @default(auto()) @map("_id") @db.ObjectId
  title   String
  excerpt String?
  image   String
  content String?
  userId  String
  view    Int?
}

model Tentang {
  id      String  @id @default(auto()) @map("_id") @db.ObjectId
  title   String
  excerpt String?
  image   String
  content String?
  userId  String
  view    Int?
}

model Berita {
  id       String  @id @default(auto()) @map("_id") @db.ObjectId
  title    String
  category String
  excerpt  String?
  image    String
  content  String?
  userId   String
  view     Int?
}

model Kontak {
  id      String  @id @default(auto()) @map("_id") @db.ObjectId
  title   String
  excerpt String?
  image   String
  content String?
  userId  String
  view    Int?
}

model Kategori {
  id     String @id @default(auto()) @map("_id") @db.ObjectId
  title  String
  userId String
}
